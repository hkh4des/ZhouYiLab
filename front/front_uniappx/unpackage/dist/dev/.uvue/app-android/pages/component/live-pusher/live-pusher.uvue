import _easycom_boolean_data from '@/components/boolean-data/boolean-data.vue';
import _easycom_enum_data from '@/components/enum-data/enum-data.vue';
import { ItemType } from '@/components/enum-data/enum-data-types';
const __sfc__ = defineComponent({
    __name: 'live-pusher',
    setup(__props): any | null {
        const __ins = getCurrentInstance()!;
        const _ctx = __ins.proxy as InstanceType<typeof __sfc__>;
        const _cache = __ins.renderCache;
        const context = ref(null as LivePusherContext | null);
        const instance = ref(null as ComponentPublicInstance | null);
        const url = ref("rtmp://test");
        const beauty = ref(0);
        const whiteness = ref(0);
        const remoteMirror = ref(false);
        const localMirror = ref("auto");
        const devicePosition = ref("front");
        const mode = ref("SD");
        const autoFocus = ref(true);
        const muted = ref(false);
        const orientation = ref("orientation");
        const enableCamera = ref(true);
        const enableMic = ref(true);
        const audioQuality = ref("high");
        const minBitrate = ref(200);
        const maxBitrate = ref(1000);
        const audioVolumeType = ref("media");
        const aspect = ref("3:4");
        const backgroundMute = ref(false);
        const waitingImage = ref("");
        const zoom = ref(1);
        const videoWidth = ref(0);
        const videoHeight = ref(0);
        const fps = ref(0);
        const isPermissionGranted = ref(false);
        const isUrlSet = ref(true);
        const initState = ref(true);
        const connectedState = ref(false);
        const stopState = ref(false);
        const previewState = ref(true);
        onReady(() => {
            instance.value = getCurrentInstance()?.proxy;
            const permissions = ["android.permission.CAMERA", "android.permission.RECORD_AUDIO"];
            UTSAndroid.requestSystemPermission(UTSAndroid.getUniActivity()!, permissions, (allRight: boolean, grantedList: string[]) => {
                if (allRight) {
                    isPermissionGranted.value = true;
                }
            }, (doNotAskAgain: boolean, grantedList: string[]) => { });
        });
        const statechange = (e: UniLivePusherStatechangeEvent) => {
            console.log("statechange", e, " at pages/component/live-pusher/live-pusher.uvue:123");
            switch (e.detail.code) {
                case 1002:
                    initState.value = false;
                    connectedState.value = true;
                    stopState.value = false;
                    break;
                case 3004:
                    connectedState.value = false;
                    break;
            }
        };
        const netstatus = (e: UniLivePusherNetstatusEvent) => {
            console.log("netstatus", e, " at pages/component/live-pusher/live-pusher.uvue:136");
        };
        const error = (e: UniLivePusherErrorEvent) => {
            console.log("error", e, " at pages/component/live-pusher/live-pusher.uvue:139");
        };
        const isUrlValid = (): boolean => {
            const valid = url.value != "rtmp://test";
            if (!valid) {
                uni.showToast({
                    title: "请输入推流地址",
                    icon: "none"
                });
            }
            return valid;
        };
        const start = () => {
            if (!isUrlValid())
                return;
            if (context.value == null) {
                context.value = uni.createLivePusherContext("live-pusher", instance.value);
            }
            context.value?.start({
                success: (res) => {
                    console.log("start", JSON.stringify(res), " at pages/component/live-pusher/live-pusher.uvue:158");
                },
                fail: (err) => {
                    console.log("start", JSON.stringify(err), " at pages/component/live-pusher/live-pusher.uvue:161");
                },
                complete: (res) => {
                    console.log("start", JSON.stringify(res), " at pages/component/live-pusher/live-pusher.uvue:164");
                }
            });
        };
        const pause = () => {
            if (!isUrlValid())
                return;
            if (context.value == null) {
                context.value = uni.createLivePusherContext("live-pusher", instance.value);
            }
            context.value?.pause({
                success: (res) => {
                    console.log("pause", JSON.stringify(res), " at pages/component/live-pusher/live-pusher.uvue:175");
                },
                fail: (err) => {
                    console.log("pause", JSON.stringify(err), " at pages/component/live-pusher/live-pusher.uvue:178");
                },
                complete: (res) => {
                    console.log("pause", JSON.stringify(res), " at pages/component/live-pusher/live-pusher.uvue:181");
                }
            });
        };
        const resume = () => {
            if (!isUrlValid())
                return;
            if (context.value == null) {
                context.value = uni.createLivePusherContext("live-pusher", instance.value);
            }
            context.value?.resume({
                success: (res) => {
                    console.log("resume", JSON.stringify(res), " at pages/component/live-pusher/live-pusher.uvue:192");
                },
                fail: (err) => {
                    console.log("resume", JSON.stringify(err), " at pages/component/live-pusher/live-pusher.uvue:195");
                },
                complete: (res) => {
                    console.log("resume", JSON.stringify(res), " at pages/component/live-pusher/live-pusher.uvue:198");
                }
            });
        };
        const stop = () => {
            if (!isUrlValid())
                return;
            stopState.value = true;
            if (context.value == null) {
                context.value = uni.createLivePusherContext("live-pusher", instance.value);
            }
            context.value?.stop({
                success: (res) => {
                    console.log("stop", JSON.stringify(res), " at pages/component/live-pusher/live-pusher.uvue:210");
                },
                fail: (err) => {
                    console.log("stop", JSON.stringify(err), " at pages/component/live-pusher/live-pusher.uvue:213");
                },
                complete: (res) => {
                    console.log("stop", JSON.stringify(res), " at pages/component/live-pusher/live-pusher.uvue:216");
                }
            });
        };
        const switchCamera = () => {
            if (context.value == null) {
                context.value = uni.createLivePusherContext("live-pusher", instance.value);
            }
            context.value?.switchCamera({
                success: (res) => {
                    console.log("switchCamera", JSON.stringify(res), " at pages/component/live-pusher/live-pusher.uvue:226");
                },
                fail: (err) => {
                    console.log("switchCamera", JSON.stringify(err), " at pages/component/live-pusher/live-pusher.uvue:229");
                },
                complete: (res) => {
                    console.log("switchCamera", JSON.stringify(res), " at pages/component/live-pusher/live-pusher.uvue:232");
                }
            });
        };
        const snapshot = () => {
            if (context.value == null) {
                context.value = uni.createLivePusherContext("live-pusher", instance.value);
            }
            context.value?.snapshot({
                success: (res) => {
                    console.log("snapshot", JSON.stringify(res), " at pages/component/live-pusher/live-pusher.uvue:242");
                },
                fail: (err) => {
                    console.log("snapshot", JSON.stringify(err), " at pages/component/live-pusher/live-pusher.uvue:245");
                },
                complete: (res) => {
                    console.log("snapshot", JSON.stringify(res), " at pages/component/live-pusher/live-pusher.uvue:248");
                }
            });
        };
        const startPreview = () => {
            if (context.value == null) {
                context.value = uni.createLivePusherContext("live-pusher", instance.value);
            }
            previewState.value = true;
            context.value?.startPreview({
                success: (res) => {
                    console.log("startPreview", JSON.stringify(res), " at pages/component/live-pusher/live-pusher.uvue:259");
                },
                fail: (err) => {
                    console.log("startPreview", JSON.stringify(err), " at pages/component/live-pusher/live-pusher.uvue:262");
                },
                complete: (res) => {
                    console.log("startPreview", JSON.stringify(res), " at pages/component/live-pusher/live-pusher.uvue:265");
                }
            });
        };
        const stopPreview = () => {
            if (context.value == null) {
                context.value = uni.createLivePusherContext("live-pusher", instance.value);
            }
            previewState.value = false;
            context.value?.stopPreview({
                success: (res) => {
                    console.log("stopPreview", JSON.stringify(res), " at pages/component/live-pusher/live-pusher.uvue:276");
                },
                fail: (err) => {
                    console.log("stopPreview", JSON.stringify(err), " at pages/component/live-pusher/live-pusher.uvue:279");
                },
                complete: (res) => {
                    console.log("stopPreview", JSON.stringify(res), " at pages/component/live-pusher/live-pusher.uvue:282");
                }
            });
        };
        const localMirrorItemTypes = [{ "value": 0, "name": "auto(前置摄像头镜像，后置摄像头不镜像)" } as ItemType, { "value": 1, "name": "enable(前后置摄像头均镜像)" } as ItemType, { "value": 2, "name": "disable(前后置摄像头均不镜像)" } as ItemType] as ItemType[];
        const localMirrorItems = ["auto", "enable", "disable"];
        const modeItemTypes = [{ "value": 0, "name": "SD(标清)" } as ItemType, { "value": 1, "name": "HD(高清)" } as ItemType, { "value": 2, "name": "FHD(超清)" } as ItemType] as ItemType[];
        const modeItems = ["SD", "HD", "FHD"];
        const orientationItemTypes = [{ "value": 0, "name": "vertical" } as ItemType, { "value": 1, "name": "horizontal" } as ItemType] as ItemType[];
        const orientationItems = ["vertical", "horizontal"];
        const aspectItemTypes = [{ "value": 0, "name": "3:4" } as ItemType, { "value": 1, "name": "9:16" } as ItemType] as ItemType[];
        const aspectItems = ["3:4", "9:16"];
        const audioQualityItemTypes = [{ "value": 0, "name": "high(高音质(48KHz))" } as ItemType, { "value": 1, "name": "low(低音质(16KHz))" } as ItemType] as ItemType[];
        const audioQualityItems = ["high", "low"];
        const devicePositionItemTypes = [{ "value": 0, "name": "front" } as ItemType, { "value": 1, "name": "back" } as ItemType] as ItemType[];
        const devicePositionItems = ["front", "back"];
        const onUrlComfirm = (event: UniInputConfirmEvent) => {
            let value = event.detail.value;
            if (value == '')
                return;
            context.value = null;
            isUrlSet.value = false;
            setTimeout(() => {
                url.value = value;
                isUrlSet.value = true;
            }, 200);
            console.log("url ->", value, " at pages/component/live-pusher/live-pusher.uvue:308");
        };
        const onMinBitrateComfirm = (event: UniInputConfirmEvent) => {
            let value = event.detail.value;
            if (value == '')
                return;
            minBitrate.value = parseInt(value);
            console.log("min-bitrate ->", value, " at pages/component/live-pusher/live-pusher.uvue:314");
        };
        const onMaxBitrateComfirm = (event: UniInputConfirmEvent) => {
            let value = event.detail.value;
            if (value == '')
                return;
            maxBitrate.value = parseInt(value);
            console.log("max-bitrate ->", value, " at pages/component/live-pusher/live-pusher.uvue:320");
        };
        const onWaitingImageComfirm = (event: UniInputConfirmEvent) => {
            let value = event.detail.value;
            if (value == '')
                return;
            waitingImage.value = value;
            console.log("waiting-image ->", value, " at pages/component/live-pusher/live-pusher.uvue:326");
        };
        const onZoomComfirm = (event: UniInputConfirmEvent) => {
            let value = event.detail.value;
            if (value == '')
                return;
            zoom.value = parseInt(value);
            console.log("zoom ->", value, " at pages/component/live-pusher/live-pusher.uvue:332");
        };
        const onBeautyChange = (event: UniSliderChangeEvent) => {
            beauty.value = event.detail.value;
            console.log("beauty ->", beauty.value, " at pages/component/live-pusher/live-pusher.uvue:336");
        };
        const onWhitenessChange = (event: UniSliderChangeEvent) => {
            whiteness.value = event.detail.value;
            console.log("whiteness ->", whiteness.value, " at pages/component/live-pusher/live-pusher.uvue:340");
        };
        const onEnableCameraChange = (value: boolean) => {
            enableCamera.value = value;
            console.log("enable-camera ->", enableCamera.value, " at pages/component/live-pusher/live-pusher.uvue:344");
        };
        const onEnableMicChange = (value: boolean) => {
            enableMic.value = value;
            console.log("enable-mic ->", enableMic.value, " at pages/component/live-pusher/live-pusher.uvue:348");
        };
        const onMutedChange = (value: boolean) => {
            muted.value = value;
            console.log("muted ->", muted.value, " at pages/component/live-pusher/live-pusher.uvue:352");
        };
        const onBackgroundMuteChange = (value: boolean) => {
            backgroundMute.value = value;
            console.log("background-mute ->", backgroundMute.value, " at pages/component/live-pusher/live-pusher.uvue:356");
        };
        const onAutoFocusChange = (value: boolean) => {
            autoFocus.value = value;
            console.log("auto-focus ->", backgroundMute.value, " at pages/component/live-pusher/live-pusher.uvue:360");
        };
        const onRemoteMirrorChange = (value: boolean) => {
            remoteMirror.value = value;
            console.log("remote-mirror ->", remoteMirror.value, " at pages/component/live-pusher/live-pusher.uvue:364");
        };
        const onLocalMirrorChange = (value: number) => {
            localMirror.value = localMirrorItems[value];
            console.log("local-mirror ->", localMirrorItems[value], " at pages/component/live-pusher/live-pusher.uvue:368");
        };
        const onDevicePositionChange = (value: number) => {
            devicePosition.value = devicePositionItems[value];
            console.log("device-position ->", devicePosition.value, " at pages/component/live-pusher/live-pusher.uvue:372");
        };
        const onModeChange = (value: number) => {
            mode.value = modeItems[value];
            console.log("mode ->", modeItems[value], " at pages/component/live-pusher/live-pusher.uvue:376");
        };
        const onAspectChange = (value: number) => {
            aspect.value = aspectItems[value];
            console.log("aspect ->", aspectItems[value], " at pages/component/live-pusher/live-pusher.uvue:380");
        };
        const onOrientationChange = (value: number) => {
            orientation.value = orientationItems[value];
            console.log("orientation ->", orientation.value, " at pages/component/live-pusher/live-pusher.uvue:384");
        };
        const onAudioQualityChange = (value: number) => {
            audioQuality.value = audioQualityItems[value];
            console.log("audio-quality ->", audioQuality.value, " at pages/component/live-pusher/live-pusher.uvue:388");
        };
        return (): any | null => {
            const _component_slider = resolveComponent("slider");
            const _component_boolean_data = resolveEasyComponent("boolean-data", _easycom_boolean_data);
            const _component_enum_data = resolveEasyComponent("enum-data", _easycom_enum_data);
            return createElementVNode("view", utsMapOf({ class: "uni-flex-item" }), [
                isTrue(unref(isPermissionGranted) && unref(isUrlSet))
                    ? createElementVNode("live-pusher", utsMapOf({
                        key: 0,
                        id: "live-pusher",
                        class: "live-pusher",
                        url: unref(url),
                        beauty: unref(beauty),
                        whiteness: unref(whiteness),
                        "remote-mirror": unref(remoteMirror),
                        "local-mirror": unref(localMirror),
                        "device-position": unref(devicePosition),
                        mode: unref(mode),
                        "auto-focus": unref(autoFocus),
                        muted: unref(muted),
                        orientation: unref(orientation),
                        "enable-camera": unref(enableCamera),
                        "enable-mic": unref(enableMic),
                        "audio-quality": unref(audioQuality),
                        "min-bitrate": unref(minBitrate),
                        "max-bitrate": unref(maxBitrate),
                        "audio-volume-type": unref(audioVolumeType),
                        aspect: unref(aspect),
                        "background-mute": unref(backgroundMute),
                        "waiting-image": unref(waitingImage),
                        zoom: unref(zoom),
                        onStatechange: statechange,
                        onNetstatus: netstatus,
                        onError: error
                    }), null, 40 /* PROPS, NEED_HYDRATION */, ["url", "beauty", "whiteness", "remote-mirror", "local-mirror", "device-position", "mode", "auto-focus", "muted", "orientation", "enable-camera", "enable-mic", "audio-quality", "min-bitrate", "max-bitrate", "audio-volume-type", "aspect", "background-mute", "waiting-image", "zoom"])
                    : createCommentVNode("v-if", true),
                createElementVNode("scroll-view", utsMapOf({ class: "uni-padding-wrap uni-common-mt uni-flex-item" }), [
                    createElementVNode("view", utsMapOf({ class: "uni-title" }), [
                        createElementVNode("text", utsMapOf({ class: "uni-title-text" }), "API示例")
                    ]),
                    createElementVNode("view", utsMapOf({ class: "uni-btn-v" }), [
                        createElementVNode("button", utsMapOf({
                            type: "primary",
                            onClick: start,
                            disabled: unref(connectedState) || !unref(previewState)
                        }), "开始推流", 8 /* PROPS */, ["disabled"])
                    ]),
                    createElementVNode("view", utsMapOf({ class: "uni-btn-v" }), [
                        createElementVNode("button", utsMapOf({
                            type: "primary",
                            onClick: pause,
                            disabled: !unref(connectedState) || !unref(previewState)
                        }), "暂停推流", 8 /* PROPS */, ["disabled"])
                    ]),
                    createElementVNode("view", utsMapOf({ class: "uni-btn-v" }), [
                        createElementVNode("button", utsMapOf({
                            type: "primary",
                            onClick: resume,
                            disabled: unref(initState) || unref(connectedState) || unref(stopState) || !unref(previewState)
                        }), "恢复推流", 8 /* PROPS */, ["disabled"])
                    ]),
                    createElementVNode("view", utsMapOf({ class: "uni-btn-v" }), [
                        createElementVNode("button", utsMapOf({
                            type: "primary",
                            onClick: stop,
                            disabled: !unref(connectedState) || !unref(previewState)
                        }), "停止推流", 8 /* PROPS */, ["disabled"])
                    ]),
                    createElementVNode("view", utsMapOf({ class: "uni-btn-v" }), [
                        createElementVNode("button", utsMapOf({
                            type: "primary",
                            onClick: switchCamera,
                            disabled: unref(stopState) || !unref(previewState)
                        }), "切换前后摄像头", 8 /* PROPS */, ["disabled"])
                    ]),
                    createElementVNode("view", utsMapOf({ class: "uni-btn-v" }), [
                        createElementVNode("button", utsMapOf({
                            type: "primary",
                            onClick: snapshot,
                            disabled: unref(stopState) || !unref(previewState)
                        }), "快照", 8 /* PROPS */, ["disabled"])
                    ]),
                    createElementVNode("view", utsMapOf({ class: "uni-btn-v" }), [
                        createElementVNode("button", utsMapOf({
                            type: "primary",
                            onClick: startPreview,
                            disabled: unref(stopState) || unref(previewState)
                        }), "开启摄像头预览", 8 /* PROPS */, ["disabled"])
                    ]),
                    createElementVNode("view", utsMapOf({ class: "uni-btn-v" }), [
                        createElementVNode("button", utsMapOf({
                            type: "primary",
                            onClick: stopPreview,
                            disabled: unref(stopState) || !unref(previewState)
                        }), "关闭摄像头预览", 8 /* PROPS */, ["disabled"])
                    ]),
                    createElementVNode("view", utsMapOf({ class: "uni-title" }), [
                        createElementVNode("text", utsMapOf({ class: "uni-title-text" }), "属性示例")
                    ]),
                    createElementVNode("input", utsMapOf({
                        class: "input margin-10",
                        type: "string",
                        placeholder: "设置推流地址",
                        onConfirm: onUrlComfirm
                    }), null, 32 /* NEED_HYDRATION */),
                    createElementVNode("input", utsMapOf({
                        class: "input margin-10",
                        type: "string",
                        placeholder: "设置最小码率",
                        onConfirm: onMinBitrateComfirm
                    }), null, 32 /* NEED_HYDRATION */),
                    createElementVNode("input", utsMapOf({
                        class: "input margin-10",
                        type: "string",
                        placeholder: "设置最大码率",
                        onConfirm: onMaxBitrateComfirm
                    }), null, 32 /* NEED_HYDRATION */),
                    createElementVNode("input", utsMapOf({
                        class: "input margin-10",
                        type: "string",
                        placeholder: "设置进入后台时推流的等待画面",
                        onConfirm: onWaitingImageComfirm
                    }), null, 32 /* NEED_HYDRATION */),
                    createElementVNode("input", utsMapOf({
                        class: "input margin-10",
                        type: "string",
                        placeholder: "设置焦距",
                        onConfirm: onZoomComfirm
                    }), null, 32 /* NEED_HYDRATION */),
                    createElementVNode("view", utsMapOf({ class: "margin-10" }), [
                        createElementVNode("text", null, "美颜, 取值范围0-9(iOS取值范围为1), 0表示关闭"),
                        createVNode(_component_slider, utsMapOf({
                            min: 0,
                            max: 9,
                            onChange: onBeautyChange
                        }))
                    ]),
                    createElementVNode("view", utsMapOf({ class: "margin-10" }), [
                        createElementVNode("text", null, "美白, 取值范围0-9(iOS取值范围为1), 0表示关闭"),
                        createVNode(_component_slider, utsMapOf({
                            min: 0,
                            max: 9,
                            onChange: onWhitenessChange
                        }))
                    ]),
                    createVNode(_component_boolean_data, utsMapOf({
                        title: "设置是否静音",
                        defaultValue: unref(muted),
                        onChange: onMutedChange
                    }), null, 8 /* PROPS */, ["defaultValue"]),
                    createVNode(_component_boolean_data, utsMapOf({
                        title: "设置是否自动聚焦",
                        defaultValue: unref(autoFocus),
                        onChange: onAutoFocusChange
                    }), null, 8 /* PROPS */, ["defaultValue"]),
                    createVNode(_component_boolean_data, utsMapOf({
                        title: "设置进入后台时是否静音",
                        defaultValue: unref(backgroundMute),
                        onChange: onBackgroundMuteChange
                    }), null, 8 /* PROPS */, ["defaultValue"]),
                    createVNode(_component_boolean_data, utsMapOf({
                        title: "设置推流画面是否镜像",
                        defaultValue: unref(remoteMirror),
                        onChange: onRemoteMirrorChange
                    }), null, 8 /* PROPS */, ["defaultValue"]),
                    createVNode(_component_enum_data, utsMapOf({
                        title: "设置本地预览画面是否镜像",
                        items: localMirrorItemTypes,
                        onChange: onLocalMirrorChange
                    })),
                    createVNode(_component_enum_data, utsMapOf({
                        title: "设置使用前置或后置摄像头",
                        items: devicePositionItemTypes,
                        onChange: onDevicePositionChange
                    })),
                    createVNode(_component_enum_data, utsMapOf({
                        title: "设置推流视频模式",
                        items: modeItemTypes,
                        onChange: onModeChange
                    })),
                    createVNode(_component_enum_data, utsMapOf({
                        title: "设置视频宽高比例",
                        items: aspectItemTypes,
                        onChange: onAspectChange
                    })),
                    createVNode(_component_enum_data, utsMapOf({
                        title: "设置画面方向",
                        items: orientationItemTypes,
                        onChange: onOrientationChange
                    })),
                    createVNode(_component_enum_data, utsMapOf({
                        title: "设置音质",
                        items: audioQualityItemTypes,
                        onChange: onAudioQualityChange
                    }))
                ])
            ]);
        };
    }
});
export default __sfc__;
const GenPagesComponentLivePusherLivePusherStyles = [utsMapOf([["live-pusher", padStyleMapOf(utsMapOf([["width", "100%"], ["height", "50%"]]))], ["input", padStyleMapOf(utsMapOf([["height", 40], ["backgroundImage", "none"], ["backgroundColor", "#FFFFFF"], ["paddingTop", 8], ["paddingRight", 13], ["paddingBottom", 8], ["paddingLeft", 13]]))], ["margin-10", padStyleMapOf(utsMapOf([["marginTop", 10], ["marginRight", 10], ["marginBottom", 10], ["marginLeft", 10]]))]])];
//# sourceMappingURL=live-pusher.uvue.map