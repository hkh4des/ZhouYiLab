import _easycom_page_head from '@/components/page-head/page-head.vue';
const __sfc__ = defineComponent({
    __name: 'is-simulator',
    setup(__props): any | null {
        const __ins = getCurrentInstance()!;
        const _ctx = __ins.proxy as InstanceType<typeof __sfc__>;
        const _cache = __ins.renderCache;
        const isSimulator = ref(false);
        isSimulator.value = uni.isSimulator();
        return (): any | null => {
            const _component_page_head = resolveEasyComponent("page-head", _easycom_page_head);
            return createElementVNode("scroll-view", utsMapOf({
                style: normalizeStyle(utsMapOf({ "flex": "1" }))
            }), [
                createElementVNode("view", null, [
                    createVNode(_component_page_head, utsMapOf({ title: 'isSimulator' })),
                    createElementVNode("view", utsMapOf({
                        class: "uni-common-mt",
                        style: normalizeStyle(utsMapOf({ "justify-content": "center", "align-items": "center" }))
                    }), [
                        createElementVNode("text", null, toDisplayString(unref(isSimulator) ? "当前设备是模拟器" : "当前设备不是模拟器"), 1 /* TEXT */),
                        createElementVNode("text", null, "\n注：有些模拟器会故意伪装为真机，此时识别可能会不准确")
                    ], 4 /* STYLE */)
                ])
            ], 4 /* STYLE */);
        };
    }
});
export default __sfc__;
const GenPagesAPIIsSimulatorIsSimulatorStyles = [];
//# sourceMappingURL=is-simulator.uvue.map